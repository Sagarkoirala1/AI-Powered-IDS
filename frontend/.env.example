# URL of the Express backend (backend/src/server.js)
VITE_API_URL=http://localhost:5000/api

# How often (ms) the dashboard/alerts views poll the backend for updates.
# The backend doesn't push over sockets yet, so this is real-time-by-polling.
VITE_POLL_INTERVAL=5000
